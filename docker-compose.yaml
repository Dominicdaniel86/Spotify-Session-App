services:
  nginx:
    build:
      context: ./
      dockerfile: server/Dockerfile
    volumes:
      - ./frontend/public:/usr/share/nginx/
    ports:
      - 80:80
    networks:
      - spotify-session-network

networks:
  spotify-session-network:
    driver: bridge
